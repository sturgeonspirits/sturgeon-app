# v2.0 — updated 2026-04-25
# changes: Removed broken scheduled-sync-menu function block. The Netlify
#          scheduled-functions runtime never executed it under @netlify/plugin-nextjs.
#          Daily menu sync is now driven by Supabase pg_cron — see
#          supabase/cron/setup-daily-menu-sync.sql.
#          v1.1 — TEMP every-minute schedule diagnostic (now obsolete).
#          v1.0 — initial Netlify config with Next.js plugin + scheduled function.

# Netlify configuration for sturgeon-spirits-app.
#
# The @netlify/plugin-nextjs plugin handles the Next.js build and
# all server-rendered + API routes (including /api/sync-menu, which
# the Supabase pg_cron job hits on schedule).

[[plugins]]
  package = "@netlify/plugin-nextjs"
